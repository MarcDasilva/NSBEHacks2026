{
  "name": "burn-registry-contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:testnet": "hardhat run scripts/deploy.js --network xrplevm-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network xrplevm-mainnet"
  },
  "devDependencies": {
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "ethers": "^6.13.0"
  }
}
